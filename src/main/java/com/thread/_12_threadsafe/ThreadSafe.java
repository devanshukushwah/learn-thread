package com.thread._12_threadsafe;

/**
 * To make the code thread-safe, you need to ensure that shared data is accessed in a controlled manner
 * to avoid race conditions or inconsistent states/result
 * */
public interface ThreadSafe {
}
